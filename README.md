# Personal Skills

This repository is a source repo for personal Codex skills.

`npx skills` expects skills in the `skills/<skill-name>/` layout, with each skill
providing a `SKILL.md` file. The first skill in this repo is:

- `skills/init-knowledge-directory`
